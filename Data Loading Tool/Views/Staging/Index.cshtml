@model IEnumerable<Data_Loading_Tool.Models.GeneralStagingModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateStaging")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TableName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.TableName, "DatasetDetails", new { datasetID = item.TableID })
        </td>        
        <td>
            @Html.ActionLink("Upload Data", "UploadToStaging", new { tableID = item.TableID })
        </td>
        <td>
            @Html.ActionLink("Create Measure", "CreateMeasure", "Dataset", new { stagingDatasetID = item.TableID }, new { })
        </td>
        <td>
            @Html.ActionLink("Create Dimension", "CreateDimension", "Dataset", new { stagingDatasetID = item.TableID }, new { })
        </td>
    </tr>
}

</table>
